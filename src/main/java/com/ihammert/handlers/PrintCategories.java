package com.ihammert.handlers;

import com.ihammert.entities.CategoryEntity;
import com.ihammert.inMemoryDatabase.CategoryInMemoryDatabase;
import java.util.ArrayList;

public class PrintCategories {

    public static void handle() {
        /*
        CategoryInMemoryDatabase categoryUnMemoryDataBase = new CategoryInMemoryDatabase();
        ArrayList<CategoryEntity> categories = categoryUnMemoryDataBase.getAll();

        if (categories.isEmpty()) {
            System.out.println("vazio ");
        } else {
            System.out.println("->Lista de categorias: ");
            for (CategoryEntity category : categories) {
                System.out.println("    Categoria: " + category.getCategoryName() + " - iD: " + category.getId());
            }
        }
        */
    }
}
